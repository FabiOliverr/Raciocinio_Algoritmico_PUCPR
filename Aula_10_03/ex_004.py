# ==========================================
# NÍVEL 1: O Detetive de Dados
# ==========================================

# 1. Imagine o sistema de uma clínica médica. Escreva abaixo qual Tipo de Dado
# (Inteiro, Real, Caractere ou Lógico) você usaria para cada variável:
# A) tipo_sanguineo (ex: "O+") ->
# B) frequencia_cardiaca (ex: 85) ->
# C) em_jejum (ex: Sim/Não) ->
# D) peso_paciente (ex: 72.5) ->

# A) usariamos o tipo caractere pois ele é um documento e não vamos fazer calculos com o mesmo.
# B) frequência cardíaca usariamos o tipo inteiro para calcular, por que não usariamos o tipo real? pois não existe meio batimento cardíaco.
# C) Em_jejum usaríamos um operador lógico pois ele está ou não está, logo usariamos um tipo boolean.
# D) peso_paciente usariamos o tipo real pois calcularemos as gramas também.